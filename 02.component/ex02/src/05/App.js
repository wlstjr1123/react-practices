import React, { Fragment } from 'react';
import styles from './assets/css/App.scss'
export default function() {

    return (
      <div className={styles.App}>
         <h1 className={styles.Header}>{`05. SASS & SCSS (css loader options: {modules: true})`}</h1> 
      </div>
    )
}